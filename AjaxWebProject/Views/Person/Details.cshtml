@model AjaxWebProject.Models.Person

@{ViewBag.Title = "Details";}

<h2>Details</h2>

<div>
    <h4>Person</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.BirthDate)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.BirthDate)
        </dd>

    </dl>
</div>

@* ---------- Part 1: Simple Ajax From Controller ---------- *@
@Ajax.ActionLink(linkText: "If you click here I use Ajax to get information from the PersonController.",
    actionName: "HitMe",
    ajaxOptions: new AjaxOptions
    {
        UpdateTargetId = "ajaxtarget",
        InsertionMode = InsertionMode.InsertAfter
    })
<div id="ajaxtarget"></div>

@* ---------- Part 2: Partial View ---------- *@
@Ajax.ActionLink(
    linkText: "Part 2 with a car.",
    actionName: "_Create",
    controllerName: "Car",
    routeValues: new {id = Model.ID},
    ajaxOptions: new AjaxOptions
    {
        UpdateTargetId = "ajaxpartialtarget",
        InsertionMode = InsertionMode.Replace
    })
<div id="ajaxpartialtarget"></div>

@* ---------- Part 3: Hide Partial View (in a crappy way) ---------- *@
@Ajax.ActionLink(
    linkText: "Hide stuff.",
    actionName: "_HideCreate",
    controllerName: "Car",
    ajaxOptions: new AjaxOptions
    {
        UpdateTargetId = "ajaxpartialtarget"
    })

<br/>
@* ---------- Part 4: Partial View With Form ---------- *@
@Ajax.ActionLink(
    linkText: "Part 4 with a car form.",
    actionName: "_CreateV2",
    controllerName: "Car",
    routeValues: new { id = Model.ID },
    ajaxOptions: new AjaxOptions
    {
        UpdateTargetId = "ajaxsecondpartialtarget",
        InsertionMode = InsertionMode.Replace
    })
<div id="ajaxsecondpartialtarget"></div>